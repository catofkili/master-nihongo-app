#!/bin/sh
ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
NODE_BIN="$HOME/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node"

if [ ! -x "$NODE_BIN" ]; then
  NODE_BIN="node"
fi

NODE_PATH="$ROOT_DIR/tools/realm_node/node_modules" exec "$NODE_BIN" "$ROOT_DIR/tools/moji_realm_dump.cjs" "$@"
